Personal Midnight Commander configuration.

# Keymap

`mc.keymap` goes to `~/.config/mc`

# Macros

Content of the `macros` folder goes to `~/.local/share/mc/mcedit/macros.d`

`mc.macros` goes to `~/.local.share`

# Syntax

Content of the `syntax` folder goes to `/usr/share/mc/syntax`